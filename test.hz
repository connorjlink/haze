function byte add = (byte x, byte y)
{
    return x + y;
}

function byte sub = (byte x, byte y)
{
    return x - y;
}

function nvr main = ()
{
    byte foo = add(1, 2); // 1 + 2 = 3
    byte bar = sub(10, 5); // 10 - 5 = 5
    byte baz = foo + bar; // 8

    print(baz);
    return 255;
}

// main:
//   copy r0, #1
//   save &0000, r0
//   call test
//   exit
// test:
//   load r0, &0000
//   exit
