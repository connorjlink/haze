#ifndef HAZEOS_STD_H
#define HAZEOS_STD_H

#include <stddef.h>
#include <stdint.h>

size_t max(size_t, size_t);
size_t min(size_t, size_t);

size_t strlen(const char*);
char* strcpy(char*, const char*);

#endif 
